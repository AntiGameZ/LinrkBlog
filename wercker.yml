box: debian
build:
  steps:
    - arjen/hugo-build:
deploy:
  steps:
    - leipert/git-push:
        gh_oauth: $GIT_TOKEN
        repo: AntiGameZ/antigamez.github.io
        branch: master
        basedir: public